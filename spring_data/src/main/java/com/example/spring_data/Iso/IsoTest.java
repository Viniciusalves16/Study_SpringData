package com.example.spring_data.Iso;

import java.time.LocalDateTime;
import java.time.ZoneOffset;
import java.time.format.DateTimeFormatter;

//public class IsoTest {
//    public static void main(String[] args) {
//        // Obter a data e hora atual em UTC
//        LocalDateTime dataHoraAtualUTC = LocalDateTime.now(ZoneOffset.UTC);
//
//        // Criar um objeto DateTimeFormatter para o padrão ISO 8601
//        DateTimeFormatter formatter = DateTimeFormatter.ofPattern("yyyy-MM-dd'T'HH:mm:ss'Z'");
//
//        // Formatando a data e hora no padrão ISO 8601 com UTC
//        String dataFormatadaUTC = dataHoraAtualUTC.format(formatter);
//
//        System.out.println("Data formatada no padrão ISO 8601 com UTC: " + dataFormatadaUTC);
//    }
//}
